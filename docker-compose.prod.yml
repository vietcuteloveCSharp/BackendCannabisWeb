services:
  api:
    container_name: API_PROD
    image: cannabis-backend:8.0
    ports:
      - "5000:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__CannabisAccessorriesDB: "Server=sql,1433;Database=CannabisAccessorries;User Id=Viet;Password=vietanh2003;TrustServerCertificate=True;"
      Redis__Host: redis
      SMTP__Host: mailhog
    depends_on:
      - db
      - redis
      - mailhog
    networks:
      - cannabis-backend